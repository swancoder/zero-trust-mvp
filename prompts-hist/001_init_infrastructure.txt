# Role: Senior Java Architect / Zero Trust Expert
# Context: Project "ZTE-Lightweight" (MVP). Java 21, Gradle (Kotlin DSL), Docker, Keycloak, PostgreSQL.

## Instructions:
1. **Chain of Thought:** Before any action, output your reasoning process in a "THOUGHTS" block. Explain WHY you choose specific libraries or structures.
2. **Self-Criticism:** After proposing a solution but before implementation, output a "CRITIQUE" block identifying potential risks or over-engineering.
3. **ADR Protocol:** For every fundamental decision (Project structure, Auth flow, DB schema), create a file in `./docs/adr/ADR-XXX-name.md`. Include: Context, Decision, Alternatives, CoT, and Self-Critique.
4. **History:** Save this current prompt into `./prompts-hist/001_init_infrastructure.txt`.

## Tasks:
1. **Project Scaffold:**
   - Initialize a Gradle multi-module project (Root + `gateway-service` + `auth-library`).
   - Use Java 21 and Spring Boot 3.4+.
   - Add dependencies for Spring Cloud Gateway, PostgreSQL, Flyway, and Keycloak Adapter (OIDC).

2. **Infrastructure (Docker):**
   - Create a `docker-compose.yml` with:
     - PostgreSQL (with a dedicated DB for ZTE).
     - Keycloak (use `quay.io/keycloak/keycloak:latest` in dev mode).
   - Create a `setup-env.sh` script (or a Claude Skill) to verify if Docker containers are healthy.

3. **Skill Creation:**
   - Create a custom skill `project-health-check` that runs `./gradlew build` and checks if Keycloak/Postgres ports are open.

4. **Git Protocol:**
   - Initialize git. Create a `.gitignore` for Java/Gradle/IntelliJ.
   - After successful setup and health check, commit to `main` with message: "chore: initial project structure and docker infra".

## Output Requirement:
- Display your Chain of Thoughts clearly.
- Perform Self-Critique on the chosen Gradle structure.
- Generate the first ADR for the "Architecture Pattern (Gateway vs Sidecar)".

---
Saved: 2026-04-02
Model: claude-sonnet-4-6
